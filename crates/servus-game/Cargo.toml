[package]
name = "servus-game"
version.workspace = true
edition.workspace = true
default-run = "servus-game"

[dependencies]
bevy = { version = "0.19", default-features = false, features = ["2d", "default_font", "ui"] }
servus-content = { path = "../servus-content" }
servus-sim = { path = "../servus-sim" }

[lints]
workspace = true
